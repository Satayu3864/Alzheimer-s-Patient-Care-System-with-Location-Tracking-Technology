package com.example.testcode  // ตรวจสอบให้ตรงกับชื่อ package ที่คุณใช้

import io.flutter.embedding.android.FlutterActivity

class MainActivity: FlutterActivity() {
    // ไม่มีฟังก์ชันอะไรเพิ่มเติมเพราะ FlutterActivity จะจัดการให้ทั้งหมด
}
